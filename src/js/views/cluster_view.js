define('ClusterView',[
  'backbone'
  ],
  function(
    Backbone
  ){

    var ClusterView = Backbone.View.extend({

      initialize: function(data) {

      },

    });

    return ClusterView;
});
